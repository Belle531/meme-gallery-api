{
  "name": "meme-gallery-api",
  "version": "1.0.0",
  "description": "A REST API for managing memes built for Netlify deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build complete - using Netlify Functions'",
    "dev": "netlify dev",
    "deploy": "netlify deploy",
    "deploy:prod": "netlify deploy --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "memes",
    "express",
    "netlify",
    "serverless"
  ],
  "author": "Cassandra Moore",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.1"
  }
}
